# Dev (.env.local): Preview-Token (zeigt Entwürfe, Visual Editor)
# Produktion: Public-Token als Environment Variable in Vercel eintragen
# (Project Settings -> Environments -> Production), sonst crasht die
# Seite beim Start (main.ts wirft einen Fehler, wenn der Token fehlt).
VITE_STORYBLOK_TOKEN=
